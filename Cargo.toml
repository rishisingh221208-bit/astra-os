[workspace]
members = [
    "kernel",
    "drivers",
    "aura-shell",
    "tests"
]
resolver = "2"

[profile.release]
opt-level = 3         
lto = true            
panic = "abort"       
codegen-units = 1     

